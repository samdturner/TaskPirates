<div id="signup">
  <div class="rating">

    <h3 class="voyages-text">
      How was ye experience wit' th' scurvy pirate?
    </h3>

    <form class="feedback-form">
      <div class="stars-container">
        <% for(var i = 5; i >= 1; i--) { %>
          <% var id = "e" + i %>
            <input name="rating" id="<%= id %>"
                    type="radio"
                    value="<%= i %>"></a>
            <label for="<%= id %>">☆</label>
        <% } %>
      </div>

      <textarea class="field-textarea form--block-input review-textarea"
                name="comment"
                placeholder="EXAMPLE: There’s a restaurant on 3rd St."
                rows="4">
      </textarea>

      <button class="btn-fld btn-primary btn btn-submit-review">
              Submit
      </button>
    </form>

  </div>
</div>
